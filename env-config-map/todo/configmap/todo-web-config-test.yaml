apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-web-config-test
  namespace: env-or-configmap
data:
  config.json: |-
    {
      "ConfigController": {
        "Enabled" : false
      },
      "Database" : {
        "Provider" : "Postgres"
      }
    }