apiVersion: v1
kind: Service
metadata:
  name: todo-db
  namespace: stateful-set
  labels:
    kiamol: ch08
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: todo-db
  clusterIP: None