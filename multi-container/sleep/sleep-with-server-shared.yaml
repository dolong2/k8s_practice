apiVersion: apps/v1
kind: Deployment
metadata:
  name: sleep
  namespace: multi-container
  labels:
    kiamol: ch07
spec:
  selector:
    matchLabels:
      app: sleep
  template:
    metadata:
      labels:
        app: sleep
        version: shared
    spec:
      shareProcessNamespace: true
      containers:
        - name: sleep
          image: kiamol/ch03-sleep
        - name: server
          image: kiamol/ch03-sleep
          command: ['sh', '-c', "while true; do echo -e 'HTTP/1.1 200 OK\nContent-Type: text/plain\nContent-Length: 7\n\nkiamol' | nc -l -p 8080; done"]
          ports:
            - containerPort: 8080
              name: http
# kubectl exec deploy/sleep -c sleep -n multi-container -- ps
# 위의 명령을 통해서 확인해보면 모든 컨테이너의 프로세스를 확인할 수 있다.
# 위의 매니페스트를 배치하기 전과 후를 비교하면 명확함